


/*const ResultadosBusqueda = () => {
    const { resultadosBusqueda } = useBusqueda();
  
    return (
      <div>
        {resultadosBusqueda.map((personaje) => (
          <div key={personaje.id}>
            <h2>{personaje.name}</h2>
            <img src={personaje.image} alt={personaje.name} />
          </div>
        ))}
      </div>
    );
  }*/